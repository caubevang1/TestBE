server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cinemaweb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: cinema_user         # không dùng root
    password: "0000"     # mật khẩu user mới
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none             # hoặc 'update' / 'validate' tùy ý
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  jackson:
    date-format: yyyy-MM-dd
    time-format: HH:mm:ss

  mail:
    host: smtp.gmail.com
    port: 587
    username: buihaiphupng6@gmail.com
    password: mdfabuwjbybeuaal
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

jwt:
  signerKey: "fAhsFQLtyhPPawzKmGDEU/IDeBpV7nGnIaQIWYZCzh8qGSb+ETsbFOoMfwfNsUgA"

management:
  endpoints:
    web:
      exposure:
        include: "*"
